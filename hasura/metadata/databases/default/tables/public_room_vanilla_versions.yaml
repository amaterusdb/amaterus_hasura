table:
  name: room_vanilla_versions
  schema: public
object_relationships:
  - name: room
    using:
      foreign_key_constraint_on: room_id
  - name: vanilla_version
    using:
      foreign_key_constraint_on: vanilla_version_id
select_permissions:
  - role: anonymous
    permission:
      columns:
        - created_at
        - updated_at
        - id
        - room_id
        - vanilla_version_id
      filter: {}
      allow_aggregations: true
    comment: ""
