table:
  name: match_result_player_roles
  schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - player_role_order_index
        - created_at
        - updated_at
        - id
        - match_id
        - person_id
        - player_role_id
        - player_role_name
      filter: {}
      allow_aggregations: true
    comment: ""
