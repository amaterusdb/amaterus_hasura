table:
  name: match_mods
  schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - created_at
        - id
        - match_id
        - mod_id
        - mod_version_id
        - updated_at
      filter: {}
      allow_aggregations: true
    comment: ""
