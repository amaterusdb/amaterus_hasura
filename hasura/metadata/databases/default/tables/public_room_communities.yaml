table:
  name: room_communities
  schema: public
object_relationships:
  - name: community
    using:
      foreign_key_constraint_on: community_id
  - name: room
    using:
      foreign_key_constraint_on: room_id
select_permissions:
  - role: anonymous
    permission:
      columns:
        - created_at
        - updated_at
        - community_id
        - id
        - room_id
      filter: {}
    comment: ""
