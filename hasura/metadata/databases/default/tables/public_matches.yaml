table:
  name: matches
  schema: public
object_relationships:
  - name: room
    using:
      foreign_key_constraint_on: room_id
array_relationships:
  - name: players
    using:
      foreign_key_constraint_on:
        column: match_id
        table:
          name: match_players
          schema: public
