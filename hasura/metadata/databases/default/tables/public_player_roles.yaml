table:
  name: player_roles
  schema: public
object_relationships:
  - name: mod
    using:
      foreign_key_constraint_on: mod_id
select_permissions:
  - role: anonymous
    permission:
      columns:
        - created_at
        - id
        - is_vanilla
        - mod_id
        - name
        - updated_at
      filter: {}
      allow_aggregations: true
    comment: ""
