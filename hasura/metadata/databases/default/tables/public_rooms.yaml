table:
  name: rooms
  schema: public
array_relationships:
  - name: matches
    using:
      foreign_key_constraint_on:
        column: room_id
        table:
          name: matches
          schema: public
