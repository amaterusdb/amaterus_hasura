table:
  name: persons
  schema: public
array_relationships:
  - name: room_persons
    using:
      foreign_key_constraint_on:
        column: person_id
        table:
          name: room_persons
          schema: public
  - name: room_youtube_lives
    using:
      foreign_key_constraint_on:
        column: person_id
        table:
          name: room_youtube_lives
          schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - name
        - created_at
        - updated_at
        - id
      filter: {}
      allow_aggregations: true
    comment: ""
